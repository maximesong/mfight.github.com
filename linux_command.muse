#title Linux命令行

<contents>

对于linux命令行还不是很熟悉，那就日积月累，聚沙成塔吧。

* ldd
作用 ::
打印出共享库依赖。
用法 ::
ldd [OPTION]... FILE...

* df
作用 ::
打印磁盘的使用情况
用法 ::
df [OPTION]... [FILE]...

* ssh-keygen
作用 ::
生成，管理和转换ssh的授权密钥
简略用法 ::
ssh-keygen -t _type_ [-C _comment_]

_type_ 的值决定了生成的key的种类，可以是rsa1、rsa、dsa等。
